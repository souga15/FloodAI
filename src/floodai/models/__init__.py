"""Model wrappers. XGBoost is the only algorithm implemented in v0.1; the
interface is deliberately thin so LightGBM/CatBoost baselines can be added
later without touching training/evaluation code."""
