"""Evaluation and metrics. This subpackage enforces, in code, that headline
metrics can only be computed from data tagged as held-out — see metrics.py
for the DataProvenance mechanism that makes this a runtime guarantee."""
